config:
  target: "http://localhost:3000"  
  phases:
    - duration: 30  # Run the test for 30 seconds
      arrivalRate: 5  # 5 users per second

scenarios:
  - flow:
      - get:
          url: "/users"  # Change based on your API
